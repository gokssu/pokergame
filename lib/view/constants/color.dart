import 'package:flutter/material.dart';

class ColorConstants {
  static ColorConstants instance = ColorConstants._init();
  ColorConstants._init();
  final white = Color(0xffffffff);
  final black = Color(0xff000000);
  final blackSoft = Color(0xB0090303);
  final backgroudColor=Color.fromRGBO(0, 83, 3, 1.0);

  final primaryColor = Color(0xffffffff);
  final accentColor = Color(0xffcb000a);
  final focusColor = Color.fromRGBO(1, 156, 7, 1.0);
  final blueColor = Color(0xff0058c4);

  final textTitle = Color.fromRGBO(224, 224, 224, 1.0);
  final textSubtitle = Color.fromRGBO(167, 167, 167, 1.0);
  final hintColor = Color.fromRGBO(105, 103, 103, 1.0);
  final button = Color.fromRGBO(250, 62, 62, 1.0);
  final dividerColor = Color.fromRGBO(106, 104, 105, 1.0);

  final graStart = Color.fromRGBO(1, 156, 7, 1.0);
  final graEnd = Color.fromRGBO(0, 134, 6, 1.0);

  final graStart2 = Color.fromRGBO(254, 254, 254, 1.0);
  final graEnd2 = Color.fromRGBO(216, 216, 216, 1.0);

}
